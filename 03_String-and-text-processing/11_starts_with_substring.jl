# Program to check if a string starts with a specific substring

# Define a string
str = "Julia Programming"

# Check if the string starts with "Julia"
starts_with = startswith(str, "Julia")

# Print the result
println("Starts with 'Julia'? ", starts_with)

# Output:
# Starts with 'Julia'? true
