# Program to split a string into an array of substrings

# Define a string
str = "apple,banana,cherry"

# Split the string by commas
split_arr = split(str, ",")

# Print the resulting array
println("Split Array: ", split_arr)

# Output:
# Split Array: ["apple", "banana", "cherry"]
