# Program to pad a string to a specific length

# Define a string
str = "Hello"

# Pad the string with spaces on the right to a length of 10
padded_str = lpad(str, 10)

# Print the padded string
println("Padded String: '", padded_str, "'")

# Output:
# Padded String: '     Hello'
