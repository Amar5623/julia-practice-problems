# Program to check if a string ends with a specific substring

# Define a string
str = "Learning Julia"

# Check if the string ends with "Julia"
ends_with = endswith(str, "Julia")

# Print the result
println("Ends with 'Julia'? ", ends_with)

# Output:
# Ends with 'Julia'? true
