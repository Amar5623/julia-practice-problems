# Program to find the index of a substring in a string

# Define a string
str = "Hello, Julia!"

# Find the index of "Julia"
index = findfirst("Julia", str)

# Print the result
println("Index of 'Julia': ", index)

# Output:
# Index of 'Julia': 8
