# Program to trim whitespace from a string

# Define a string with leading and trailing whitespace
str = "   Hello, Julia!   "

# Trim the whitespace
trimmed_str = strip(str)

# Print the result
println("Trimmed String: '", trimmed_str, "'")

# Output:
# Trimmed String: 'Hello, Julia!'
