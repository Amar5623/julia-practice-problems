# Program to check equality of two sets

# Create two sets
set1 = Set([1, 2, 3])
set2 = Set([1, 2, 3])

# Check if the sets are equal
are_equal = set1 == set2

# Print result
println("Are the sets equal? ", are_equal)

# Output:
# Are the sets equal? true
